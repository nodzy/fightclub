
$('#dialog h3').html("<i class='glyphicon glyphicon-plus'></i> Add New Fight");

$('.modal-body').html('<%= j render(@fight)  %>');

$('#dialog').modal("show");

$('#dialog').on('shown.bs.modal', function () {
	  $('.first_input').focus()
  })
  
  $('#dialog').on('hidden.bs.modal', function () {
 location.reload();
})

